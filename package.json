{
  "name": "plan-html",
  "version": "0.1.0",
  "description": "Interactive decision-deck planning — the deck UI as a single self-contained, offline HTML file (hand-edited, no build step).",
  "license": "MIT",
  "author": "Dory Zidon",
  "type": "module",
  "scripts": {
    "lint": "node scripts/lint-deck.js",
    "test:e2e": "playwright test",
    "test": "npm run lint && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "eslint": "^9.8.0"
  }
}
